# Viewport

Zoom and pan the viewport through mouse wheel or trackpad gestures, similar to browsers and design software viewport interactions.

## Install Plugin

You need to install the viewport plugin to use it. [Visit Github repository](https://github.com/leaferjs/leafer-in/tree/main/packages/viewport).

::: code-group

```sh [npm]
npm install @leafer-in/viewport
```

```sh [pnpm]
pnpm add @leafer-in/viewport
```

```sh [yarn]
yarn add @leafer-in/viewport
```

```sh [bun]
bun add @leafer-in/viewport
```

:::

Or include via script tag and access plugin features through the global variable `LeaferIN.viewport`.

::: code-group

```html [viewport.min]
<script src="https://unpkg.com/@leafer-in/viewport@2.0.9/dist/viewport.min.js"></script>
```

```html [viewport]
<script src="https://unpkg.com/@leafer-in/viewport@2.0.9/dist/viewport.js"></script>
```

<!-- If unpkg is not accessible, you can replace it with https://cdn.jsdelivr.net/npm -->

:::

## Next Steps

### [Viewport Zoom & Pan](../../../guide/advanced/viewport.md)
